e:24px;line-height:1.35;margin:0 0 14px}
.case p{margin:0;color:var(--muted);line-height:1.8}
.case-flow{display:flex;flex-wrap:wrap;gap:8px;align-items:center;margin-top:20px}
.case-flow span{
  padding:8px 10px;border-radius:10px;background:#f3f8fa;border:1px solid #e1eaf0;
  font-size:12px;font-weight:800;color:#536675
}
.case-flow i{font-style:normal;color:var(--cyan);font-weight:900}

/* CTA */
.cta{
  text-align:center;padding:92px 0 110px;
  background:linear-gradient(180deg,#f8fbfc,#eef7fb)
}
.cta-box{
  max-width:900px;margin:0 auto;padding:54px 28px;border-radius:32px;background:#fff;
  border:1px solid var(--line)
}
.cta h2{margin:0;font-size:clamp(34px,4.8vw,58px);line-height:1.18;letter-spacing:-.04em}
.cta p{max-width:700px;margin:18px auto 28px;color:var(--muted);line-height:1.8}
.btn{
  display:inline-flex;align-items:center;justify-content:center;min-height:54px;
  padding:0 28px;border-radius:999px;
  background:linear-gradient(90deg,var(--cyan),var(--blue),var(--purple));
  color:#fff;font-weight:900;box-shadow:0 14px 28px rgba(77,145,234,.16)
}
.note{margin-top:14px;font-size:12px;color:#8a949d}

@media(max-width:820px){
  section{padding:72px 0}
  .wrap{width:min(100% - 28px,1120px)}
  .hero{min-height:auto;padding:28px 0 64px}
  .brand{margin-bottom:36px}
  .hero-grid{grid-template-columns:1fr;gap:34px}
  .hero h1{font-size:46px}
  .hero p{font-size:16px}
  .hero-visual{min-height:320px}
  .cards-3,.start-grid,.case-grid,.benefit-grid{grid-template-columns:1fr}
  .recommend-grid{grid-template-columns:1fr}
  .section-title{font-size:34px}
  .section-lead{text-align:left;font-size:16px}
  .center{text-align:left}
  .tabs{justify-content:flex-start}
  .tab-panel ul{grid-template-columns:1fr}
  .row{grid-template-columns:1fr}
  .row dt{padding-bottom:7px}
  .row dd{padding-top:7px}
  .flow-list::before{left:24px}
  .flow-item{grid-template-columns:50px 1fr;gap:14px}
  .flow-num{width:50px;height:50px}
  html.js .slide-left,html.js .slide-right{transform:translateY(24px)}
  .cta{padding:72px 0 90px}
  .cta-box{padding:40px 20px}
}


/* ===== Varied CSS scroll-driven animation =====
   JavaScript-free / mobile Safari friendly when scroll timelines are supported.
   Unsupported browsers keep all content visible. */

.motion{opacity:1;transform:none;filter:none}

.scroll-progress{
  position:fixed;
  inset:0 auto auto 0;
  width:100%;
  height:3px;
  z-index:9999;
  pointer-events:none;
}
.scroll-progress-bar{
  width:100%;
  height:100%;
  transform:scaleX(0);
  transform-origin:left center;
  background:linear-gradient(90deg,var(--cyan),var(--blue),var(--purple));
}

/* Hero ambient motion: always visible */
.ai-window{animation:heroFloat 5.2s ease-in-out infinite}
.hero-orb{animation:heroOrb 8s ease-in-out infinite alternate}
@keyframes heroFloat{
  0%,100%{transform:translate(-50%,-50%) translateY(0) rotate(0deg)}
  50%{transform:translate(-50%,-50%) translateY(-10px) rotate(.35deg)}
}
@keyframes heroOrb{
  from{transform:translate(0,0) scale(1)}
  to{transform:translate(-24px,22px) scale(1.10)}
}

/* Section heading accent */
.section-title{
  position:relative;
}
.section-title::after{
  content:"";
  display:block;
  width:72px;
  height:3px;
  margin:18px auto 0;
  border-radius:999px;
  background:linear-gradient(90deg,var(--cyan),var(--purple));
  opacity:.75;
  transform-origin:center;
}
@media(max-width:820px){
  .section-title::after{margin-left:0}
}

/* Decorative motion details */
.num,.flow-num,.tick{
  transition:box-shadow .3s ease;
}
.purpose-card,.rec,.flow-copy,.tab-panel,.overview,.start-card,.benefit,.case,.cta-box{
  will-change:transform,opacity,filter,clip-path;
}

/* Scroll-driven effects */
@supports (animation-timeline:view()) {

  /* Global document progress */
  .scroll-progress-bar{
    animation:pageProgress linear both;
    animation-timeline:scroll(root block);
  }
  @keyframes pageProgress{
    from{transform:scaleX(0)}
    to{transform:scaleX(1)}
  }

  /* HERO copy: gentle zo