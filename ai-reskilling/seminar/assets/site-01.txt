<!doctype html>
<html lang="ja">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
<meta name="robots" content="noindex,nofollow,noarchive">
<title>Meta Heroes｜無料社内AIセミナー概要（アニメーション多彩版）</title>
<style>
:root{
  --ink:#141a21;
  --muted:#697684;
  --line:#e6edf3;
  --bg:#ffffff;
  --soft:#f7fbfd;
  --soft2:#eef8fb;
  --cyan:#31c6d8;
  --blue:#4d91ea;
  --purple:#735be0;
  --dark:#121923;
}
*{box-sizing:border-box}
html{scroll-behavior:smooth}
body{
  margin:0;
  font-family:-apple-system,BlinkMacSystemFont,"Segoe UI","Hiragino Kaku Gothic ProN","Yu Gothic",Meiryo,sans-serif;
  color:var(--ink);
  background:#fff;
  overflow-x:hidden;
}
a{text-decoration:none;color:inherit}
img{max-width:100%;display:block}
.wrap{width:min(1120px,calc(100% - 40px));margin:0 auto}
section{padding:96px 0}
.eyebrow{
  font-size:12px;
  font-weight:800;
  letter-spacing:.12em;
  color:#6f7c88;
  margin-bottom:14px;
}
.section-title{
  margin:0;
  font-size:clamp(32px,4.4vw,56px);
  line-height:1.16;
  letter-spacing:-.04em;
}
.section-lead{
  margin:18px auto 0;
  max-width:760px;
  color:var(--muted);
  font-size:17px;
  line-height:1.9;
}
.center{text-align:center}
.gradient{
  background:linear-gradient(90deg,var(--cyan),var(--blue),var(--purple));
  -webkit-background-clip:text;background-clip:text;color:transparent
}

/* HERO */
.hero{
  padding:38px 0 80px;
  min-height:92vh;
  display:flex;
  align-items:center;
  position:relative;
  overflow:hidden;
}
.hero::before{
  content:"";
  position:absolute;inset:0;
  background:
    radial-gradient(circle at 15% 20%,rgba(49,198,216,.12),transparent 26%),
    radial-gradient(circle at 85% 20%,rgba(115,91,224,.10),transparent 24%);
  pointer-events:none;
}
.brand{
  display:flex;align-items:center;gap:10px;
  font-weight:800;font-size:18px;margin-bottom:54px
}
.brand-mark{
  width:30px;height:30px;border-radius:10px;
  background:linear-gradient(135deg,var(--cyan),var(--purple));
  box-shadow:0 8px 20px rgba(77,145,234,.18)
}
.hero-grid{
  display:grid;grid-template-columns:1.1fr .9fr;gap:60px;align-items:center
}
.hero h1{
  font-size:clamp(44px,6.5vw,78px);
  line-height:1.08;
  letter-spacing:-.055em;
  margin:0 0 24px
}
.hero p{
  color:var(--muted);
  font-size:18px;line-height:1.9;
  max-width:720px;margin:0 0 28px
}
.tags{display:flex;flex-wrap:wrap;gap:10px}
.tag{
  padding:10px 14px;border:1px solid var(--line);border-radius:999px;
  background:#fff;font-size:13px;font-weight:700;color:#4a5662
}
.hero-visual{
  position:relative;
  min-height:420px;
  border-radius:34px;
  background:linear-gradient(145deg,#f7fcfd,#f2f4ff);
  border:1px solid #e7edf3;
  overflow:hidden;
}
.hero-orb{
  position:absolute;width:210px;height:210px;border-radius:50%;
  background:linear-gradient(135deg,rgba(49,198,216,.4),rgba(115,91,224,.32));
  filter:blur(2px);right:-30px;top:-20px;
}
.ai-window{
  position:absolute;left:50%;top:50%;transform:translate(-50%,-50%);
  width:76%;background:#fff;border:1px solid var(--line);border-radius:22px;
  box-shadow:0 28px 60px rgba(24,40,61,.10);overflow:hidden
}
.window-top{height:42px;border-bottom:1px solid var(--line);display:flex;gap:6px;align-items:center;padding:0 14px}
.window-top span{width:8px;height:8px;border-radius:50%;background:#d6dce2}
.chat{padding:22px}
.bubble{
  padding:14px 16px;border-radius:16px;background:#f6f9fb;color:#56616d;
  font-size:13px;line-height:1.65;margin-bottom:10px
}
.bubble.ai{background:linear-gradient(90deg,#edfafa,#f3efff);color:#27313a}
.cursor{
  width:8px;height:18px;background:var(--cyan);display:inline-block;vertical-align:-4px;
  animation:blink 1s steps(2,end) infinite
}
@keyframes blink{50%{opacity:0}}

/* PURPOSE */
.soft{background:var(--soft)}
.cards-3{display:grid;grid-template-columns:repeat(3,1fr);gap:18px;margin-top:44px}
.purpose-card{
  padding:28px;border:1px solid var(--line);border-radius:24px;background:#fff;
  min-height:220px
}
.num{
  widt