h:42px;height:42px;border-radius:14px;display:grid;place-items:center;
  background:linear-gradient(135deg,#e9fbfc,#f1efff);
  color:#43627a;font-weight:900;margin-bottom:18px
}
.purpose-card h3{margin:0 0 10px;font-size:21px}
.purpose-card p{margin:0;color:var(--muted);line-height:1.75;font-size:15px}

/* RECOMMEND */
.recommend-grid{display:grid;grid-template-columns:repeat(2,1fr);gap:14px;margin-top:40px}
.rec{
  padding:20px 22px;border:1px solid var(--line);border-radius:18px;
  display:flex;gap:14px;background:#fff
}
.tick{
  flex:0 0 28px;width:28px;height:28px;border-radius:50%;
  display:grid;place-items:center;background:linear-gradient(135deg,var(--cyan),var(--blue));
  color:#fff;font-weight:900;font-size:14px
}
.rec strong{font-size:16px;line-height:1.65}

/* FLOW */
.flow-list{max-width:850px;margin:48px auto 0;position:relative}
.flow-list::before{
  content:"";position:absolute;left:28px;top:34px;bottom:34px;width:2px;
  background:linear-gradient(var(--cyan),var(--purple));opacity:.35
}
.flow-item{
  display:grid;grid-template-columns:58px 1fr;gap:20px;align-items:start;
  padding:18px 0
}
.flow-num{
  position:relative;z-index:2;
  width:58px;height:58px;border-radius:50%;
  display:grid;place-items:center;background:#fff;border:1px solid #dfe9f0;
  font-weight:900;color:#48627a;box-shadow:0 8px 18px rgba(24,40,61,.05)
}
.flow-copy{
  padding:22px 24px;border:1px solid var(--line);border-radius:20px;background:#fff
}
.flow-copy h3{margin:0 0 7px;font-size:20px}
.flow-copy p{margin:0;color:var(--muted);line-height:1.75}

/* JOB TABS */
.tabs-shell{margin-top:38px}
.tab-radio{position:absolute;opacity:0;pointer-events:none}
.tabs{display:flex;gap:10px;flex-wrap:wrap;justify-content:center;margin-bottom:20px}
.tab{
  padding:11px 18px;border-radius:999px;border:1px solid #dfe6ec;background:#fff;
  font-weight:800;color:#53606c;cursor:pointer
}
.tab-panel{display:none;max-width:860px;margin:0 auto;border:1px solid var(--line);border-radius:24px;background:#fff;padding:30px}
.tab-panel h3{margin:0 0 18px;font-size:22px}
.tab-panel ul{margin:0;padding-left:20px;display:grid;grid-template-columns:repeat(2,1fr);gap:10px 28px;color:#5b6875;line-height:1.7}
#sales:checked ~ .tabs label[for="sales"],
#admin:checked ~ .tabs label[for="admin"],
#manager:checked ~ .tabs label[for="manager"]{
  background:linear-gradient(90deg,var(--cyan),var(--blue),var(--purple));
  border-color:transparent;color:#fff
}
#sales:checked ~ .tab-panels .sales,
#admin:checked ~ .tab-panels .admin,
#manager:checked ~ .tab-panels .manager{display:block}

/* OVERVIEW */
.overview{
  max-width:880px;margin:44px auto 0;border:1px solid var(--line);
  border-radius:24px;background:#fff;overflow:hidden
}
.row{display:grid;grid-template-columns:180px 1fr;border-bottom:1px solid var(--line)}
.row:last-child{border-bottom:none}
.row dt,.row dd{margin:0;padding:18px 22px}
.row dt{background:#f8fbfc;font-weight:800;color:#42505d}
.row dd{color:#5f6d79;line-height:1.7}

/* START FLOW */
.start-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:16px;margin-top:42px}
.start-card{
  padding:24px;border:1px solid var(--line);border-radius:20px;background:#fff;position:relative
}
.start-card small{color:#7d8791;font-weight:900;letter-spacing:.08em}
.start-card h3{margin:10px 0 8px;font-size:19px}
.start-card p{margin:0;color:var(--muted);line-height:1.7;font-size:14px}

/* BENEFIT */
.benefit-grid{display:grid;grid-template-columns:1fr 1fr;gap:20px;margin-top:44px}
.benefit{
  border:1px solid var(--line);border-radius:26px;padding:28px;background:#fff
}
.benefit h3{margin:0 0 20px;font-size:22px}
.benefit ul{margin:0;padding-left:20px;color:#5d6975;line-height:2}

/* CASE */
.case-grid{display:grid;grid-template-columns:1fr 1fr;gap:20px;margin-top:44px}
.case{
  border:1px solid var(--line);border-radius:26px;padding:30px;background:#fff
}
.case-label{font-size:12px;font-weight:900;letter-spacing:.08em;color:#71808c;margin-bottom:12px}
.case h3{font-siz