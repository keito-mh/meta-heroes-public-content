<!DOCTYPE html>
<html lang="ja">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<title>Meta Heroes｜無料社内AIセミナー（LLMO×AIエージェント活用版）</title>
<style>
:root{
  --navy:#08162f;
  --navy2:#0e2348;
  --cyan:#11cacb;
  --blue:#3fa8d8;
  --purple:#7b5cd6;
  --pink:#a34fc5;
  --white:#ffffff;
  --text:#122033;
  --muted:#66758b;
  --bg:#f5f8fc;
  --line:#dfe7f0;
}
*{box-sizing:border-box}
html{scroll-behavior:smooth}
body{
  margin:0;
  font-family:-apple-system,BlinkMacSystemFont,"Segoe UI","Hiragino Kaku Gothic ProN","Yu Gothic",Meiryo,sans-serif;
  background:var(--bg);
  color:var(--text);
  overflow-x:hidden;
}
a{text-decoration:none;color:inherit}
.hero{
  min-height:92vh;
  display:flex;
  align-items:center;
  position:relative;
  overflow:hidden;
  background:
    radial-gradient(circle at 15% 20%, rgba(17,202,203,.18), transparent 30%),
    radial-gradient(circle at 85% 30%, rgba(123,92,214,.20), transparent 28%),
    linear-gradient(135deg,var(--navy) 0%,#0a1c39 45%,#101d3b 100%);
  color:white;
  padding:72px 20px;
}
.hero::before,.hero::after{
  content:"";
  position:absolute;
  border:1px solid rgba(255,255,255,.08);
  border-radius:50%;
  width:420px;height:420px;
  animation:float 10s ease-in-out infinite;
}
.hero::before{right:-150px;top:-140px}
.hero::after{left:-220px;bottom:-210px;animation-delay:-3s}
@keyframes float{
  0%,100%{transform:translateY(0) scale(1)}
  50%{transform:translateY(18px) scale(1.04)}
}
.wrap{width:min(1120px,100%);margin:0 auto;position:relative;z-index:2}
.brand{
  display:flex;align-items:center;gap:10px;
  font-weight:800;letter-spacing:.02em;
  margin-bottom:34px;
}
.brand-dot{
  width:14px;height:14px;border-radius:50%;
  background:linear-gradient(135deg,var(--cyan),var(--purple));
  box-shadow:0 0 22px rgba(17,202,203,.55)
}
.badge{
  display:inline-flex;align-items:center;gap:8px;
  padding:9px 14px;border-radius:999px;
  background:rgba(255,255,255,.08);
  border:1px solid rgba(255,255,255,.12);
  color:#dce7f7;font-size:14px;font-weight:700;
  backdrop-filter:blur(12px)
}
.hero h1{
  font-size:clamp(40px,7vw,78px);
  line-height:1.08;
  margin:22px 0 18px;
  letter-spacing:-.04em;
  max-width:920px;
}
.gradient-text{
  background:linear-gradient(90deg,#fff 0%,#a9f7f7 45%,#d9c7ff 100%);
  -webkit-background-clip:text;background-clip:text;color:transparent;
}
.hero p{
  max-width:760px;
  font-size:clamp(17px,2.2vw,22px);
  line-height:1.85;
  color:#c9d8ea;
  margin:0 0 34px;
}
.cta-row{display:flex;flex-wrap:wrap;gap:12px}
.btn{
  display:inline-flex;align-items:center;justify-content:center;
  padding:16px 22px;border-radius:14px;font-weight:800;
  transition:.25s ease;cursor:pointer;border:none;font-size:16px;
}
.btn-primary{
  background:linear-gradient(90deg,var(--cyan),#4dc8ee);
  color:#06162f;box-shadow:0 14px 35px rgba(17,202,203,.20)
}
.btn-primary:hover{transform:translateY(-2px)}
.btn-ghost{border:1px solid rgba(255,255,255,.18);color:#fff;background:rgba(255,255,255,.05)}
.hero-note{margin-top:20px;color:#90a6c2;font-size:13px}
.orb{
  position:absolute;
  width:10px;height:10px;border-radius:50%;
  background:var(--cyan);filter:blur(.2px);
  box-shadow:0 0 24px rgba(17,202,203,.7);
  animation:orbmove 7s linear infinite;
}
.orb.o1{top:18%;left:73%}
.orb.o2{top:65%;left:82%;animation-delay:-2s;background:#aa83ff}
.orb.o3{top:78%;left:14%;animation-delay:-4s}
@keyframes orbmove{
  0%{transform:translate(0,0);opacity:.3}
  50%{transform:translate(18px,-25px);opacity:1}
  100%{transform:translate(0,0);opacity:.3}
}
section{padding:90px 20px}
.section-title{
  font-size:clamp(30px,4.2vw,48px);
  line-height:1.2;margin:0 0 16px;
  letter-spacing:-.025em
}
.section-lead{
  margin:0 0 42px;color:var(--muted);
  font-size:18px;line-height:1.8;max-width:780px
}
.dark{background:var(--navy);color:white}
.dark .section-lead{color:#b9c7da}
.grid{display:grid;gap:18px}
.grid-3{grid-template-columns:repeat(3,1fr)}
.grid-2{grid-template-columns:repeat(2,1fr)}
.card{
  background:white;border:1px solid var(--line);
  border-radius:22px;padding:28px;
  box-shadow:0 14px 40px rgba(7,27,59,.06)
}
.dark .card{
  background:#102343;border-color:#1d365d;
  box-shadow:none
}
.icon{
  width:50px;height:50px;border-radius:15px;
  display:flex;align-items:center;justify-content:center;
  background:linear-gradient(135deg,rgba(17,202,203,.18),rgba(123,92,214,.14));
  font-size:23px;margin-bottom:18px
}
.card h3{margin:0 0 10px;font-size:21px}
.card p{margin:0;color:var(--muted);line-height:1.75}
.dark .card p{color:#b9c8da}
.problem-list{
  display:grid;grid-template-columns:repeat(2,1fr);gap:14px;margin-top:32px
}
.problem{
  padding:20px 22px;border-radius:16px;background:#fff;border:1px solid var(--line);
  display:flex;gap:14px;align-items:flex-start
}
.problem strong{display:block;margin-bottom:4px}
.check{
  flex:0 0 26px;width:26px;height:26px;border-radius:50%;
  background:linear-gradient(135deg,var(--cyan),var(--blue));
  color:#06213a;display:flex;align-items:center;justify-content:center;
  font-weight:900
}
.flow{
  display:grid;grid-template-columns:repeat(5,1fr);gap:12px;
  margin-top:44px;position:relative
}
.flow::before{
  content:"";position:absolute;left:7%;right:7%;top:42px;height:2px;
  background:linear-gradient(90deg,var(--cyan),var(--purple));opacity:.45
}
.step{
  position:relative;z-index:2;text-align:center
}
.step-num{
  width:84px;height:84px;margin:0 auto 16px;border-radius:50%;
  background:linear-gradient(135deg,#123055,#193d66);
  border:1px solid #2c527e;
  display:flex;align-items:center;justify-content:center;
  font-size:28px;font-weight:900;color:#fff;
  box-shadow:0 0 0 8px var(--navy)
}
.step h4{margin:0 0 7px;font-size:17px}
.step p{font-size:13px;color:#aebed3;line-height:1.6;margin:0}
.tabs-shell{position:relative}
.tab-radio{
  position:absolute;
  opacity:0;
  width:1px;
  height:1px;
  overflow:hidden;
  pointer-events:none;
}
.tabs{display:flex;flex-wrap:wrap;gap:10px;margin:26px 0 22px}
.tab{
  display:inline-flex;
  align-items:center;
  justify-content:center;
  padding:11px 16px;
  border-radius:999px;
  border:1px solid #cbd8e7;
  background:white;
  cursor:pointer;
  font-weight:800;
  color:#42526a;
  transition:.2s;
  -webkit-tap-highlight-color:transparent;
  user-select:none;
}
.tab:active{transform:scale(.985)}
.tab-panel{
  display:none;
  background:white;
  border:1px solid var(--line);
  border-radius:22px;
  padding:30px;
  box-shadow:0 14px 38px rgba(7,27,59,.06)
}
.tab-panel ul{margin:16px 0 0;padding-left:18px;line-height:2}

/* Pure CSS tabs: works even when JavaScript is blocked by mobile preview */
#tab-sales:checked ~ .tabs label[for="tab-sales"],
#tab-admin:checked ~ .tabs label[for="tab-admin"],
#tab-manager:checked ~ .tabs label[for="tab-manager"]{
  color:white;
  border-color:transparent;
  background:linear-gradient(90deg,var(--blue),var(--purple));
}
#tab-sales:checked ~ .tab-panels #sales-panel,
#tab-admin:checked ~ .tab-panels #admin-panel,
#tab-manager:checked ~ .tab-panels #manager-panel{
  display:block;
  animation:fadeUp .3s ease;
}
@keyframes fadeUp{from{opacity:.25;transform:translateY(5px)}to{opacity:1;transform:none}}
.agenda{
  display:grid;grid-template-columns:90px 1fr;gap:0;
  border:1px solid var(--line);border-radius:22px;overflow:hidden;background:#fff
}
.agenda-time{
  padding:22px 16px;background:#f2f7fb;font-weight:900;color:#244564;border-bottom:1px solid var(--line)
}
.agenda-content{
  padding:22px;border-bottom:1px solid var(--line)
}
.agenda-content strong{font-size:18px}
.agenda-content span{display:block;color:var(--muted);margin-top:5px;line-height:1.6}
.agenda > :nth-last-child(-n+2){border-bottom:none}
.result-box{
  background:
    radial-gradient(circle at 85% 0%,rgba(17,202,203,.16),transparent 30%),
    linear-gradient(135deg,#0c1d38,#122a4c);
  color:white;border-radius:28px;padding:38px;border:1px solid #1a3a62
}
.result-box h3{font-size:28px;margin:0 0 14px}
.result-box p{color:#c8d7e8;line-height:1.8;margin:0}
.result-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:12px;margin-top:24px}
.result-pill{
  padding:18px;border-radius:16px;background:rgba(255,255,255,.06);
  border:1px solid rgba(255,255,255,.08);font-weight:700;line-height:1.5
}
.quote{
  text-align:center;padding:34px 20px;
  font-size:clamp(24px,3vw,36px);font-weight:900;
  color:#18324f
}
.quote span{color:var(--blue)}
.offer{
  background:linear-gradient(135deg,#eefcff 0%,#f6f1ff 100%);
  border:1px solid #d7e5f2;border-radius:28px;padding:38px
}
.offer h3{font-size:30px;margin:0 0 14px}
.offer p{color:var(--muted);line-height:1.8}
.offer-meta{display:flex;flex-wrap:wrap;gap:10px;margin-top:18px}
.meta{
  padding:10px 13px;border-radius:12px;background:white;border:1px solid #d9e5ef;
  font-weight:700;color:#34506d
}
.cta-section{
  text-align:center;background:
    radial-gradient(circle at 50% 10%,rgba(17,202,203,.16),transparent 35%),
    linear-gradient(135deg,var(--navy),#0e2142);
  color:white
}
.cta-section p{color:#b9c8da;max-width:720px;margin:0 auto 28px;line-height:1.8}
.cta-section .btn{font-size:17px}
.small-note{font-size:12px;color:#8295ad;margin-top:16px}
footer{
  padding:28px 20px;text-align:center;background:#061225;color:#8195ad;font-size:13px
}
.reveal{
  opacity:1;
  transform:none;
}
html.motion-ready .reveal{
  opacity:0;
  transform:translateY(24px);
  transition:opacity .65s ease, transform .65s ease;
}
html.motion-ready .reveal.visible{
  opacity:1;
  transform:none;
}
@media (prefers-reduced-motion: reduce){
  html.motion-ready .reveal{
    opacity:1 !important;
    transform:none !important;
    transition:none !important;
  }
}

/* ===== Scroll storytelling ===== */
.scroll-story{
  position:relative;
  padding:0;
  background:linear-gradient(180deg,#08162f 0%,#0c2142 52%,#f5f8fc 52%,#f5f8fc 100%);
}
.story-shell{
  width:min(1180px,100%);
  margin:0 auto;
  padding:92px 20px 105px;
}
.story-kicker{
  display:inline-flex;align-items:center;gap:8px;
  padding:8px 12px;border-radius:999px;
  background:rgba(17,202,203,.11);
  border:1px solid rgba(17,202,203,.24);
  color:#83efef;font-size:13px;font-weight:850;
  letter-spacing:.04em;
}
.story-title{
  max-width:780px;color:#fff;
  font-size:clamp(34px,5vw,58px);
  line-height:1.14;letter-spacing:-.035em;
  margin:18px 0 16px;
}
.story-lead{
  max-width:730px;color:#b9c8da;
  font-size:18px;line-height:1.8;margin:0 0 46px;
}
.story-track{
  display:grid;
  grid-template-columns:repeat(3,1fr);
  gap:18px;
  align-items:stretch;
}
.scene-card{
  position:relative;
  min-height:510px;
  border-radius:28px;
  overflow:hidden;
  background:#fff;
  border:1px solid rgba(255,255,255,.08);
  box-shadow:0 30px 70px rgba(5,19,42,.20);
}
.scene-top{
  min-height:285px;
  position:relative;
  overflow:hidden;
  background:linear-gradient(145deg,#102846,#16385d);
}
.scene-copy{padding:26px}
.scene-num{
  font-size:12px;font-weight:900;letter-spacing:.13em;color:var(--blue);
  margin-bottom:8px
}
.scene-copy h3{margin:0 0 9px;font-size:23px;letter-spacing:-.02em}
.scene-copy p{margin:0;color:var(--muted);line-height:1.7;font-size:15px}

/* Scene 01: messy work */
.desk{
  position:absolute;left:9%;right:9%;bottom:14%;height:104px;
  border-radius:18px;background:#173a60;border:1px solid #29537f;
  transform:perspective(500px) rotateX(58deg);
  transform-origin:bottom center;
}
.paper{
  position:absolute;width:104px;height:72px;border-radius:8px;
  background:#f6f8fc;box-shadow:0 10px 20px rgba(0,0,0,.18)
}
.paper.p1{left:12%;top:23%;transform:rotate(-14deg)}
.paper.p2{right:11%;top:31%;transform:rotate(12deg)}
.paper.p3{left:38%;top:48%;transform:rotate(4deg);width:122px}
.paper::before,.paper::after{
  content:"";position:absolute;left:12px;right:12px;height:5px;border-radius:5px;background:#cbd8e7
}
.paper::before{top:17px}.paper::after{top:31px;width:58%}
.alert{
  position:absolute;right:13%;top:12%;width:50px;height:50px;
  display:grid;place-items:center;border-radius:50%;
  background:#e96c78;color:white;font-weight:950;font-size:24px;
  box-shadow:0 0 0 9px rgba(233,108,120,.12);
  animation:pulseAlert 1.8s ease-in-out infinite
}
@keyframes pulseAlert{50%{transform:scale(1.08);box-shadow:0 0 0 16px rgba(233,108,120,.04)}}

/* Scene 02: AI assistance */
.ai-ring{
  position:absolute;left:50%;top:47%;
  width:155px;height:155px;transform:translate(-50%,-50%);
  border-radius:50%;
  background:
    radial-gradient(circle at center,#0f2848 0 35%,transparent 36%),
    conic-gradient(from 0deg,var(--cyan),var(--blue),var(--purple),var(--cyan));
  box-shadow:0 0 44px rgba(17,202,203,.26);
  animation:spinRing 8s linear infinite
}
.ai-ring::after{
  content:"AI";position:absolute;inset:18px;border-radius:50%;
  background:#102846;display:grid;place-items:center;
  color:white;font-size:38px;font-weight:950;
}
@keyframes spinRing{to{transform:translate(-50%,-50%) rotate(360deg)}}
.data-chip{
  position:absolute;padding:9px 11px;border-radius:12px;
  background:rgba(255,255,255,.09);
  border:1px solid rgba(255,255,255,.12);
  color:#d8e8f6;font-size:12px;font-weight:800;
  backdrop-filter:blur(9px);
  animation:chipFloat 3.2s ease-in-out infinite
}
.data-chip.c1{left:8%;top:18%}
.data-chip.c2{right:7%;top:24%;animation-delay:-1.1s}
.data-chip.c3{left:14%;bottom:16%;animation-delay:-2.2s}
@keyframes chipFloat{50%{transform:translateY(-9px)}}

/* Scene 03: organized output */
.dashboard-mini{
  position:absolute;left:8%;right:8%;top:14%;bottom:13%;
  border-radius:20px;background:#f8fbff;border:1px solid #cfe0ef;
  box-shadow:0 22px 35px rgba(0,0,0,.18);overflow:hidden
}
.dash-bar{height:34px;background:#eaf2f8;border-bottom:1px solid #d9e5ef}
.dash-grid{display:grid;grid-template-columns:1fr 1fr;gap:10px;padding:13px}
.metric-box{
  min-height:64px;border-radius:12px;background:#fff;border:1px solid #dce7f0;padding:10px
}
.metric-line{height:7px;background:#c9d8e5;border-radius:99px;margin-bottom:8px}
.metric-line.short{width:55%}
.chart-bars{display:flex;gap:7px;align-items:end;height:84px;padding:10px 13px}
.chart-bars span{
  flex:1;border-radius:7px 7px 3px 3px;
  background:linear-gradient(180deg,var(--cyan),var(--blue));
  animation:barGrow 2.4s ease-in-out infinite alternate
}
.chart-bars span:nth-child(1){height:32%}
.chart-bars span:nth-child(2){height:54%;animation-delay:-.3s}
.chart-bars span:nth-child(3){height:77%;animation-delay:-.7s}
.chart-bars span:nth-child(4){height:92%;animation-delay:-1.1s}
@keyframes barGrow{to{filter:brightness(1.15);transform:scaleY(1.04)}}
.done-badge{
  position:absolute;right:11%;bottom:9%;padding:10px 13px;border-radius:999px;
  background:#35b98c;color:white;font-weight:900;font-size:12px;
  box-shadow:0 10px 24px rgba(53,185,140,.24)
}

/* sticky mini journey */
.journey{
  background:#fff;
}
.journey-head{
  max-width:760px;
  margin-bottom:28px;
}
.journey-grid{
  display:grid;
  grid-template-columns:1fr;
  gap:22px;
  align-items:start
}
.journey-banner{
  position:relative;
  padding:28px;border-radius:26px;
  background:linear-gradient(145deg,#091a35,#102b50);
  color:white;overflow:hidden;
  min-height:250px
}
.journey-banner::after{
  content:"";position:absolute;width:240px;height:240px;border-radius:50%;
  right:-110px;bottom:-110px;border:1px solid rgba(17,202,203,.22)
}
.journey-banner h3{font-size:30px;margin:0 0 12px;line-height:1.25}
.journey-banner p{color:#b8c7da;line-height:1.8;margin:0;max-width:700px}
.scan-visual{
  position:relative;height:130px;margin-top:22px;border-radius:20px;
  background:#0d2343;border:1px solid #1c436e;overflow:hidden
}
.scan-line{
  position:absolute;left:0;right:0;height:2px;
  background:linear-gradient(90deg,transparent,var(--cyan),transparent);
  box-shadow:0 0 15px rgba(17,202,203,.8);
  animation:scan 2.6s ease-in-out infinite
}
@keyframes scan{0%,100%{top:18%}50%{top:80%}}
.scan-node{
  position:absolute;width:10px;height:10px;border-radius:50%;
  backgrou